# =====================================================================
# api/.htaccess
#
# No routing needed here. The frontend calls /api/index.php/route/path
# directly (PATH_INFO style). Apache resolves index.php as a real file
# on disk, sends it to FPM once, no rewrites, no loops.
#
# DunganSoft Technologies, March 2026
# =====================================================================

# Ensure PATH_INFO is passed to PHP (default on most setups)
AcceptPathInfo On
